name: Run cafe24pro parameter

on:
  workflow_dispatch:
  schedule:
    - cron: '0 23 * * *'  # 매일 오전 8시 (Asia/Seoul=UTC+9)

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run script
        env:
          CLIENT_SECRET_FILE: ${{ secrets.CLIENT_SECRET_FILE }}
          TOKEN_FILE: ${{ secrets.TOKEN_FILE }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
          WIKI_URL: ${{ secrets.WIKI_URL }}
          WIKI_PAGE_ID: ${{ secrets.WIKI_PAGE_ID }}
          WIKI_USERNAME: ${{ secrets.WIKI_USERNAME }}
          WIKI_PASSWORD: ${{ secrets.WIKI_PASSWORD }}
        run: python cafe24pro_parameter.py
